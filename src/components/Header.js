import React from 'react';

const Header = () => (
  <header className="my-2 sm:text-center max-w-md mx-auto md:max-w-2xl">
    <h1 className="text-xl font-semibold tracking-wider mb-2">
      Discover your 10 core values
    </h1>
    <p className="text-sm font-medium mb-1">
      It'll take less than 5 minutes. <span className="hidden md:inline">Remember - if everything is a core value, then nothing is really a priority.
      </span>
    </p>
  </header>
);

export default Header;